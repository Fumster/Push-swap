#include "push_swap.h"

void	pb_less_mid (stacks, mid)
{
	t_stack	st_a;
	
	st_a = stacks->stack_a;

}

void	sort_max_el (t_list *stacks, int max)
{
	int		next;
	int		mid;

	next = 1;
	mid = max / 2 + next;
	pb_less_mid(stacks, mid)
}
